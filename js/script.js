$(document).ready(function(){
	$('.form__type').slick({
		slidesToShow: 2,
		slidesToScroll: 2,
	});

	$('.chipsets__slider').slick({
		fade: true,
		speed: 300,
		cssEase: 'linear',
		dots: true,
	});
	
});

	